<resources>

    <style name="Theme.DartDL" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Edge-to-edge is handled by enableEdgeToEdge() at runtime.
             Deprecated android:statusBarColor / android:navigationBarColor removed for SDK 35 compliance. -->
    </style>

    <style name="Theme.DartDL.Dialog" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Theme.DartDL.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_custom_padded</item>
        <item name="postSplashScreenTheme">@style/Theme.DartDL</item>
    </style>

</resources>
